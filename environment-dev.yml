name: QUGaR
channels:
  - conda-forge
dependencies:
  # Explicit pins for compatibility
  - python >=3.10        # dolfinx 0.10.0 supports >=3.10 very reliably on conda-forge
  - fenics-dolfinx ==0.10.0

  - mpich
  - pyvista
  - scipy
  - liblapacke
  - openblas

  - scikit-build-core
  - cmake
  - ninja
  - catch2
  - doxygen
  - nanobind

  - sphinx
  - breathe
  - myst-parser
  - markdown

  - ipython
  - pytest
  - jupytext
  - pip
  - pip:
      - sphinx-rtd-dark-mode

